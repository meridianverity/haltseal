# Examples

Each file is one full gateway review packet. The filenames mirror the vector IDs.

Start with:

- `h01_allow_current_permit_exact_action.json`
- `h02_hold_stale_signed_head.json`
- `h04_deny_action_digest_mismatch.json`
- `h05_deny_replay_nonce_monotonic.json`
